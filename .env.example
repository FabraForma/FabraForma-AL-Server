# This file serves as an example for the required environment variables.
# For local development, you can copy this file to a new file named .env
# and fill in the actual values. The .env file should NOT be committed to git.

# A strong, randomly generated secret key is required for signing sessions and tokens.
# You can generate one using: openssl rand -hex 32
SECRET_KEY=your-super-strong-and-secret-key-here

# The message broker for Celery. Redis is recommended.
REDIS_URL=redis://localhost:6379/0

# --- For Production (Part 2 of the plan) ---
# When migrating to a more robust database like PostgreSQL, you would add its connection URI here.
# DATABASE_URI=postgresql://user:password@hostname:port/databasename
